name: CodeFlowOps Info

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Show Info
      run: |
        echo "CodeFlowOps SaaS Status"
        echo "Frontend: https://www.codeflowops.com" 
        echo "Backend: https://api.codeflowops.com"
        echo "Use ./deploy-backend.ps1 for deployment"
